import React from "react";
import Fieldset from "../Fieldset";

export const WorkingPolicy = () => {
  return (
    <Fieldset title="Working Policy" className="h-full w-full">
    </Fieldset>
  );
};
